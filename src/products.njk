---
layout: base.njk
title: Our Services
description: Explore our range of AI consultation services to find the perfect fit for your business needs.
permalink: /services/
---

<main class="container mx-auto px-4 py-12">
  <h1 class="text-3xl font-bold mb-8">Our Services</h1>
  
  <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
    {% for product in products %}
    {% if product.indexable != false %}
      <div class="border rounded-lg overflow-hidden shadow-lg hover:shadow-xl transition-shadow">
        {% if product.og_image %}
          <img 
            src="{{ product.og_image }}" 
            alt="{{ product.title }}" 
            class="w-full h-48 object-cover"
          >
        {% endif %}
        
        <div class="p-6">
          <h2 class="text-xl font-semibold mb-2">{{ product.title }}</h2>
          <p class="text-gray-600 mb-4">{{ product.description | truncate(120) }}</p>
          <div class="flex justify-between items-center">
            <span class="text-2xl font-bold">${{ product.price.toFixed(2) }}</span>
            <a 
              href="/{{ product.slug }}/" 
              class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition-colors"
            >
              Learn More
            </a>
          </div>
        </div>
      </div>
    {% endif %}
    {% endfor %}
  </div>
</main>